<li>
  <span>{{ 'blogs.article.tags' | t }}:</span>
  {% for tag in article.tags %}
  <a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag | join: ', ' }}</a>{% unless forloop.last %},{% endunless %}
  {% endfor %}
</li>
